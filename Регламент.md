# 📘 Регламент разработки и передачи исходного кода

Документ описывает единые правила оформления, структуры, архитектуры и
стиля кода для проекта. Цель - обеспечить читаемость, предсказуемость
и удобство поддержки.

------------------------

# 1. Введение

Регламент определяет:

-   структуру проекта;
-   правила оформления кода;
-   архитектуру flow‑классов;
-   требования к логированию, подсчёту токенов и конфигурации;
-   требования к инфраструктуре;
-   формат примеров использования.

Документ обязателен для всех разработчиков.

------------------------

# 2. Python и зависимости

-   проект разрабатывается и запускается на **Python 3.13**;
-   управление зависимостями - через **uv**;
-   все зависимости фиксируются в `pyproject.toml`; ручная установка
    запрещена;
-   линтинг и форматирование:
    -   **ruff** - стиль, форматирование и проверки качества;
    -   **mypy** - статическая типизация;
-   все инструменты запускаются через pre-commit;
-   код должен полностью соответствовать настройкам ruff, mypy и хуков;
-   запрещены глобальные переменные.

------------------------

# 3. Структура проекта

## 3.1 Общая структура

    project/
    │
    ├── app/                                 # Основной модуль проекта (должен быть самодостаточным)
    │   ├── outsource/                       # Исходный код приложения: бизнес-логика, LLM-вызовы, утилиты
    │   │   ├── flows/                       # Flow-классы, вызываемые из demonstration/main.py
    │   │   └── ...                          # Дополнительные модули логики (клиенты, utils, llm, parsers и т.д.)
    │   │
    │   └── consts.py                        # Параметры окружения
    │
    ├── demonstration/                       # Демонстрационный сценарий использования модуля
    │   ├── ...                              # Данные, файлы примеров, тестовые входы
    │   └── main.py                          # Точка входа для демонстрации работы проекта
    │
    ├── eksmo_src/                           # Исходные модули Eksmo (смежные типы, структуры, вспомогательные модели)
    │   └── ...
    │
    ├── .pre-commit-config.yaml              # Конфигурация pre-commit хуков
    ├── pyproject.toml                       # Зависимости и настройки инструментов
    └── README.md                            # Документация проекта

Самодостаточность директории `app/`

Директория `app/` является полностью автономным модулем проекта.
Она не должна зависеть от локальных модулей вне `app/`, за исключением импорта из: `eksmo_src`

## 3.2 Размещение кода

-   проектный код - в `app/`;
-   бизнес-логика - `app/outsource/flows/`;
-   конфигурация окружения - `app/consts.py`;
-   демонстрация - `demonstration/main.py`;
-   импорт только абсолютный.

------------------------

# 4. Стиль и оформление

## 4.1 Общие правила Python

-   обязательные типовые аннотации;
-   максимальная длина строки - 100 символов;
-   запрещено: `print`, `global`, несколько команд через `;`, а так же глобалы (константы и logger исключение)

## 4.2 Docstring

-   стиль Google, на русском;
-   типы указываются только в аннотациях.

Пример:

``` python
def translate(text: str, lang: str) -> str:
    """Перевод текста.

    Args:
        text: Текст для перевода.
        lang: Язык перевода.

    Returns:
        Переведённый текст.
    """
```

## 4.3 Логирование

-   используется loguru;
-   уровни: `debug`, `info`, `warning`, `error`.
-   используется обычный импорт глобального `logger` из loguru,
    без открытия контекста - в приложении контекст уже открыт
    и необходимые параметры прокидываются автоматически,
    путь к файлу логов вручную не указывается.

## 4.4 Именование

-   файлы и модули - snake_case;
-   директории - snake_case;
-   классы - PascalCase;
-   flow-классы - с суффиксом *Flow*;
-   переменные и функции - snake_case;
-   константы - UPPER_CASE;
-   приватные атрибуты - с префиксом `_`.

------------------------

# 5. Flow‑классы

## 5.1 Назначение

Flow - класс, выполняющий одну задачу (LLM-вызов, обработка данных и
т.п.).

## 5.2 Структура

-   один публичный асинхронный метод `run()`;
-   метод `run()` принимает параметр `total_usage`;
-   этапы выполнения:
    1.  подготовка данных;
    2.  вызов сервиса;
    3.  постобработка результата;
    4.  сохранение данных.

## 5.3 Правила

-   flow не управляет другими flow;
-   нет скрытых зависимостей;
-   flow не является god-object;
-   композиция flow - только в `main.py`.

## 5.4 Подсчёт токенов

-   каждый flow считает usage только своих вызовов;
-   результат и usage возвращаются независимо.

## 5.5 Пример Flow класса

```python
import re

from pathlib import Path

from loguru import logger

from app.consts import AI__API_KEY
from app.consts import AI__MAX_TOKENS
from app.consts import AI__MODEL
from app.consts import AI__TEMPERATURE
from app.consts import SYSTEM_PROMPT

from app.outsource.llm.llm_client import LLMClient
from eksmo_src.eksmo_types import Usage


class TranslateFlow:
    @classmethod
    async def run(
            cls,
            *,
            total_usage: Usage,
            input_file_path: Path,
            output_file_path: Path,
    ) -> None:
        """Запуск флоу LLM перевода.

        Args:
            total_usage: Аккумулятор токенов, который будет обновлён после выполнения запроса.
            input_file_path: Путь к файлу с исходным текстом для перевода.
            output_file_path: Путь к файлу, куда будет записан переведённый текст.

        Raises:
            Exception: Превышение максимального числа попыток обращения к LLM.
        """
        logger.debug('Запуск флоу LLM перевода')

        llm = LLMClient(token=AI__API_KEY)

        original_text = input_file_path.read_text()
        # Разбиваем по знакам конца предложения, сохраняя их
        chunks = re.findall(r'.+?[.!?](?:\s+|$)', original_text)

        for chunk in chunks:
            answer = await llm.ask_with_retries(
                system_prompt=SYSTEM_PROMPT,
                prompt=chunk,
                model=AI__MODEL,
                max_tokens=AI__MAX_TOKENS,
                temperature=AI__TEMPERATURE,
            )

            logger.info(f'Получен ответ от {AI__MODEL}: {answer.model_dump()}')

            total_usage.input_tokens += answer.usage
            output_file_path.write_text(answer.message)

            logger.debug('Флоу LLM перевода завершен успешно')

```


------------------------

# 6. Пример использования (main.py)

## 6.1 Назначение

`main.py` - минимальный сценарий запуска flow.

## 6.2 Правила

-   вызовы flow - последовательные;
-   для каждого flow:
    1.  создание экземпляра;
    2.  вызов `run()`;
    3.  получение результата (опционально);
    4.  сохранение данных.

## 6.3 Пример

``` python
import asyncio
from pathlib import Path

from loguru import logger

from app.outsource.flows.translate_flow import TranslateFlow
from eksmo_src.eksmo_types import Usage

DATA_DIR_PATH = Path(__file__).resolve().parent.parent / 'demonstration_data'

INPUT_FILE_PATH = DATA_DIR_PATH / 'text_for_translate.md'
OUTPUT_FILE_PATH = DATA_DIR_PATH / 'translated.md'


async def main():
    """Точка входа для всего проекта."""
    logger.info('Запуск демонстрации работы модуля')
    logger.info(f'Файл с текстом для перевода: {INPUT_FILE_PATH}')

    usage = Usage()

    await TranslateFlow.run(
        total_usage=usage, input_file_path=INPUT_FILE_PATH, output_file_path=OUTPUT_FILE_PATH
    )

    logger.info(f'Использовано токенов: {usage.model_dump()}')
    logger.info(f'Файл с результатом:{OUTPUT_FILE_PATH}')
    
asyncio.run(main())
```

---------

# 7. Контроль качества и устранение предупреждений
- весь проект регулярно проходит проверку через ruff, mypy, pre-commit и другие инструменты статического анализа;
- все выявленные предупреждения и замечания обязаны быть устранены — отключать или подавлять предупреждения (ignore / skip / disable) запрещено;
- перед передачей кода обязательна полная проверка, гарантирующая полное отсутствие предупреждений и ошибок линтеров;
- правки должны устранять первопричину, а не косметически изменять стиль;
- запрещено «затирать» проблемы фиктивными исправлениями;
- перед любым коммитом и перед передачей кода разработчик обязан выполнить полную проверку через:
```bash
    pre-commit run --all-files
```
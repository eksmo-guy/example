# AI Translation Flow
Модуль для ИИ перевода

Принимает на вход текстовый файл, преводит его с помощью OpenAI, и сохраняет результат в новый файл.

---
### Используемый стек:
🐍 Python3.13   
🐍 Pydantic2   
🐍 OpenAI
---

# 📗 1. Установка


## 1.1 Переменные окружения

### Настройки ИИ
| Имя переменной    | Значение по умолчанию           | Описание                                     |
|-------------------|---------------------------------|----------------------------------------------|
| `SYSTEM_PROMPT`   | *Не задано — требуется указать* | Системный промпт для ИИ.                     |
| `AI__API_KEY`     | *Не задано — требуется указать* | API-ключ для ИИ.                             |
| `AI__MODEL`       | *Не задано — требуется указать* | Название модели ИИ.                          |
| `AI__TEMPERATURE` | *Не задано — требуется указать* | Температура для генерации ответа ИИ.         |
| `AI__MAX_TOKENS`  | *Не задано — требуется указать* | Максимальное количество токенов в ответе ИИ. |

> Переменные окружения задаются в файле [app/consts.py](app/consts.py).

## 1.2 Установка зависимостей
### 1.2.1 Установка UV
Для Linux / macOS
```bash
    curl -LsSf https://astral.sh/uv/install.sh | sh                # Linux / macOS
```
Для Windows (PowerShell)
```bash
    powershell -c "irm https://astral.sh/uv/install.ps1 | iex"     # Windows (PowerShell)
```

### 1.2.2 Установка модулей python
```bash
    uv sync
    uv run pre-commit install
```

# 2. Демонстрация работы проекта

1) Перед запуском необходимо поместить текст для перевода в файл `demonstration/data/text_for_translate.md`.

2) Для запуска выполните команду
```python
python3 demonstration/main.py
```

3) После умпешного завершения будет создан файл с результатом `demonstration/data/translated.md`.